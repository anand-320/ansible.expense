format_version: 10
pipelines:
  backend:
    group: defaultGroup
    materials:
      git-7f8559b:
        git: https://github.com/anand-320/ansible.expense.git
        shallow_clone: false
        auto_update: true
        branch: main
    stages:
      - DEV:
          fetch_materials: true
          environment_variables:
            env: dev
          approval:
            type: manual
          jobs:
            ansible-playbook:
              tasks:
              - exec:
                  arguments:
                    - ansible.sh
                    - backend
                  command: bash
                  run_if: passed
      - QA:
          fetch_materials: true
          environment_variables:
            env: qa
          approval:
            type: manual
          jobs:
            ansible-playbook:
              tasks:
              - exec:
                  arguments:
                    - ansible.sh
                    - backend
                  command: bash
                  run_if: passed
      - STAGE:
          fetch_materials: true
          environment_variables:
            env: STAGE
          approval:
            type: manual
          jobs:
            ansible-playbook:
              tasks:
              - exec:
                  arguments:
                    - ansible.sh
                    - backend
                  command: bash
                  run_if: passed
      - PROD:
          fetch_materials: true
          environment_variables:
            env: PROD
          approval:
            type: manual
          jobs:
            ansible-playbook:
              tasks:
              - exec:
                  arguments:
                    - ansible.sh
                    - backend
                  command: bash
                  run_if: passed