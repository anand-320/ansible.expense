- name: frontend setup
  hosts: all
  become: true
  tasks:
    - name: install nginx
      ansible.builtin.dnf:
        name: nginx
        state: latest


    - name: enable nginx
      ansible.builtin.service:
        name: nginx
        state: started