- name: setup mysql
  hosts: all
  become: true
  tasks:
    - name: install mysql server
      ansible.builtin.dnf:
        name: mysql-server
        state: latest

    - name: start mysql
      ansible.builtin.service:
        name: mysqld
        state: restarted
